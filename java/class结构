# class结构

## 魔数 0XCAFEBABY u4

## 次版本号u2和主版本号u2

## 常量池
	### 常量池数量count u2
	### 数量索引从1开始，真正的数量是count-1
	### 具体的常量信息，类名、方法名、返回值

## 类的访问标识 access_flags,求并集得到，u2

## 类索引 u2,父类索引 u2，实现接口索引集n*u2

## 字段表（field_info）,用于描述接口或者类中声明的变量，字段包括类变量以及实例级变量，但不包括方法内声明的局部变量

## 方法 code标识指令、局部变量、虚拟机栈深度